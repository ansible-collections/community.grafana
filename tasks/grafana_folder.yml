---
- name: create folder
  community.grafana.grafana_folder:
    url: "{{ grafana_url }}"
    url_username: "{{ grafana_username }}"
    url_password:  "{{ grafana_password }}"
    title: "{{ folder }}"
  loop: "{{ grafana_folder }}"
  loop_control:
    loop_var: folder
  tags: folder
